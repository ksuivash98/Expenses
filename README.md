# Личный финансовый кабинет

Автономное веб-приложение для учёта личных финансов. Работает полностью офлайн в браузере.

- Frontend: HTML5, CSS3, Vanilla JS (ES Modules)
- Данные: единое хранилище **LocalStorage** (`financeApp_v1`)
- Без сервера, без авторизации, без Supabase

## Быстрый старт

Откройте `index.html` в браузере или через локальный сервер:

```bash
npx serve .
```

Приложение сразу открывает главную страницу. Регистрация и вход не нужны.

## Первый запуск

Автоматически создаются:

- текущий месяц (финансовый период);
- стандартные категории бюджета (конверты);
- пустые списки доходов, расходов, кредитов, КУслуг и целей;
- настройки по умолчанию;
- запись в истории и приветственное уведомление.

## Хранение данных

Все данные хранятся в одном объекте LocalStorage:

```js
financeApp = {
  settings: {},
  currentPeriod: {},
  financialPeriods: [],
  income: [],
  budgetCategories: [],
  budgetTransactions: [],
  expenses: [],
  credits: [],
  creditPayments: [],
  utilities: [],
  utilityPayments: [],
  goals: [],
  notifications: [],
  history: []
}
```

После любого изменения данные сохраняются автоматически. Кнопки «Сохранить» нет.

## Резервное копирование

В разделе **Настройки**:

- 📤 Экспорт всех данных в JSON
- 📥 Импорт данных из JSON
- 📄 Скачать резервную копию
- 📂 Восстановить из резервной копии
- 🗑 Очистить все данные (с двойным подтверждением)

## Разделы

| Файл | Назначение |
|------|------------|
| `index.html` | Точка входа |
| `app.js` | Логика приложения и формы |
| `storage.js` | LocalStorage, автосохранение |
| `income.js` | Доходы |
| `budget.js` | Конверты, распределение, периоды |
| `expenses.js` | Расходы |
| `credits.js` | Кредиты |
| `utilities.js` | Коммунальные услуги |
| `goals.js` | Цели |
| `analytics.js` | Аналитика и план/факт |
| `calendar.js` | Календарь платежей |
| `history.js` | История |
| `notifications.js` | Уведомления |
| `settings.js` | Настройки и бэкап |
| `charts.js` | Диаграммы Canvas |
| `ui.js` | Интерфейс |
| `utils.js` | Утилиты |
| `style.css` | Стили |

## Денежный поток

1. Добавьте доход  
2. Распределите всю свободную сумму по конвертам  
3. Расходы, кредиты, КУслуги и цели списываются только из выбранного конверта  

## Периоды

Можно переключать месяцы, закрывать текущий месяц с переносом остатков по правилам конвертов, смотреть архив и сравнивать периоды.
